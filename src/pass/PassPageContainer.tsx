import React from 'react';
import PropTypes from 'prop-types';

const PassPageContainer = () => {
  return <div>Pass Page</div>;
};

PassPageContainer.propTypes = {};

export default PassPageContainer;
